# Replace this file on the real VPS with the output of:
#   nixos-generate-config --show-hardware-config
#
# Keep the resulting file next to this host's default.nix. It holds disk UUIDs,
# filesystem mounts, and boot settings that cannot be shared across machines.
{ ... }:
{
}
